[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "yambopy"
version = "0.1.0"
authors = [
    { name = "Torsten Geirsson", email = "torsten.geirsson@outlook.com" }
]
description = "Fork from yambopy"
dependencies = []

